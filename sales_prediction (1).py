# -*- coding: utf-8 -*-
"""sales prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1HjPgiR60DnWw4Mx2u9T6vyLsnJyuheY6
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

path="/content/drive/MyDrive/csv files/Advertising.csv"
df=pd.read_csv(path)

df.head(5)

df.tail(5)

df=df.drop(columns=['Unnamed: 0'])

df

df.shape

df.info()

df.describe()

X = df.iloc[:,0:-1]

X

y=df.iloc[:,-1]

y

X.iloc[:,0]

df.head(2)

df.corr()

import seaborn as sns

sns.pairplot(data=df)
plt.show()

from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=43)

X_train

X_test

y_train

y_test

X_train = X_train.astype(int)
X_test = X_test.astype(int)
y_train = y_train.astype(int)
y_test = y_test.astype(int)

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()

X_train_scaled = sc.fit_transform(X_train)
X_test_scaled = sc.fit_transform(X_test)

from sklearn.linear_model import LinearRegression
lr = LinearRegression()

lr.fit(X_train_scaled, y_train)

y_pred = lr.predict(X_test_scaled)

y_pred

y_test

import matplotlib.pyplot as plt

plt.scatter(y_test, y_pred, c='r')

import matplotlib.pyplot as plt

plt.scatter(y_test, y_pred, c='r', label='predicted', marker='x')
plt.scatter(y_test, y_test, c='b', label='True', marker='o')
plt.xlabel('True values')
plt.ylabel('Predicted values')
plt.legend()
plt.show()

from sklearn.metrics import mean_squared_error

ms = mean_squared_error(y_test, y_pred)
ms

from sklearn.metrics import mean_absolute_error

ma = mean_squared_error(y_test, y_pred)
ma

from sklearn.metrics import r2_score

r2 = r2_score(y_test, y_pred)
r2

